package com.jungmin;

public class A_IsOldEnoughToDrink {
    public boolean isOldEnoughToDrink(int age) {
        // TODO:
        if (age > 17) {
            return true;
        } else {
            return false;
        }
    }
}
